product: jvm/lib

description: 从低代码 LSI 确定性生成 Kotlin 实体、DTO、契约和路由源码。

apply:
  - ../../build-config/common-jvm.module-template.yaml

dependencies:
  - ../lowcode-metadata: exported
  - ../constant-compiler
  - ../dto-compiler
  - ../validation-compiler

test-dependencies:
  - ../ddl-compiler
