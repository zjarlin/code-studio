product: jvm/app

description: 为没有启动类的库提供通用 Studio 开发宿主。

apply:
  - ../../build-config/common-jvm.module-template.yaml

dependencies:
  - ../../modules/development-host

settings:
  jvm:
    mainClass: site.addzero.studio.devhost.ApplicationKt
